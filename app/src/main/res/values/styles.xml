<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AlerDialogDz">
        <item name="android:background">@drawable/alert_dz</item>
        <item name="android:textColor">@color/backgroundWhite</item>
        <item name="android:textColorHint">@color/belFon</item>
        <item name="android:buttonStyle">@style/custom_button</item>
    </style>
    
    
    <style name="custom_button" parent="@android:style/Widget.Button">
        <item name="android:paddingLeft">5dp</item>
        <item name="android:paddingRight">5dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:paddingTop">3dp</item>
        <item name="android:background">@drawable/alert_btn</item>
        <item name="colorButtonNormal">@color/backgroundWhite</item>
        <item name="android:textColor">@color/backgroundWhite</item>
    </style>

</resources>